generated: '2026-09-09'
method: searched
source: >-
  npm registry search and metadata (registry.npmjs.org), PyPI, and the GitHub org search
  (2026-09-09). Ferguson's Enterprise APIs are documented only inside the approval-gated
  developer portal (developer.ferguson.com), which publishes no SDK downloads on any page
  reachable without credentials, so no first-party API client library could be found on
  any public registry.
description: >-
  Ferguson publishes no first-party API client library (no npm, PyPI, Maven Central,
  NuGet, RubyGems, Packagist, crates.io or pkg.go.dev package wraps the Ferguson
  Enterprise APIs). The only first-party package found on a public registry is a UI CSS
  framework from Ferguson's UX team, last released in 2018. It is recorded here for
  currency evidence; it is NOT an API client, so no `SDKs` pointer is wired from this
  file.
sdk_count: 0
packages:
  - language: css
    registry: npm
    name: '@ferguson-enterprises/fds-css'
    url: https://www.npmjs.com/package/@ferguson-enterprises/fds-css
    install: npm install @ferguson-enterprises/fds-css
    repository: https://github.com/FergusonUX/ferguson-design-system
    official: true
    api_client: false
    kind: design-system
    version: 1.0.6
    published: '2018-02-06'
    note: >-
      Ferguson Design System CSS framework. First-party on the evidence of the npm scope
      (@ferguson-enterprises, Ferguson's legal name is Ferguson Enterprises, LLC) and the
      publishing GitHub org FergusonUX, whose three public repos (fds-css, pt-csv-tool,
      my-products-prototype) were all last touched in 2018. Latest version 1.0.6 was
      published 2018-02-06 and nothing has shipped since — the package is dormant. It
      styles user interfaces and does not call any Ferguson API.
registries_checked:
  - registry: npm
    result: one first-party package found (design system, not an API client)
  - registry: pypi
    result: none (pypi.org/pypi/ferguson/json returned 404)
  - registry: maven-central
    result: none
  - registry: nuget
    result: none
  - registry: rubygems
    result: none
  - registry: packagist
    result: none
  - registry: crates.io
    result: none
  - registry: pkg.go.dev
    result: none
